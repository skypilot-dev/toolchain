/* Return values for sort functions used in `Array.prototype.sort()` */
import { SortComparison } from '../types';

export const SORT_HIGHER: SortComparison = -1;
export const SORT_EQUAL: SortComparison = 0;
export const SORT_LOWER: SortComparison = 1;
